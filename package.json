{
  "name": "js-tree-dom",
  "version": "0.0.1",
  "scripts": {
    "start": "node .",
    "build": "babel src -d lib",
    "test": "npm run build && intern-client config=tests/intern",
    "tests": "npm run test"
  },
  "dependencies": {
    "js-tree-diff": "git+ssh://git@github.com/tylerbwilliams/js-tree-diff.git"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-register": "^6.6.5",
    "intern": "^3.0.6"
  }
}
